import { supabase } from './auth.js';import { requireRole } from './access.js';import { mountShell } from './shell.js';
const access=await requireRole(['owner']);if(!access)throw new Error('Sin permiso');await mountShell();
const $=s=>document.querySelector(s);let products=[];const msg=(t,ok=false)=>{const e=$('#storeAdminMessage');e.textContent=t;e.className=`message show ${ok?'ok':'error'}`};
async function load(){const {data,error}=await supabase.from('store_products').select('*').order('sort_order');if(error)return msg(error.message);products=data||[];$('#adminProducts').innerHTML=products.map(p=>`<article><div><strong>${p.name}</strong><small>${p.category} · S/ ${Number(p.price).toFixed(2)} · ${p.active?'Visible':'Oculto'}</small></div><div><button data-edit="${p.id}">Editar</button><button data-delete="${p.id}">Eliminar</button></div></article>`).join('')||'<div class="empty-state">No hay productos.</div>'}
function reset(){['#productId','#productName','#productPrice','#productOldPrice','#productBadge','#productLogo'].forEach(s=>$(s).value='');$('#productCategory').value='streaming';$('#productOrder').value=0;$('#productFeatured').checked=false;$('#productSoldout').checked=false;$('#productActive').checked=true}
$('#productForm').onsubmit=async e=>{e.preventDefault();const id=$('#productId').value;const payload={name:$('#productName').value.trim(),category:$('#productCategory').value,label:$('#productCategory').selectedOptions[0].text,price:Number($('#productPrice').value),old_price:$('#productOldPrice').value?Number($('#productOldPrice').value):null,badge:$('#productBadge').value.trim(),logo_url:$('#productLogo').value.trim(),sort_order:Number($('#productOrder').value||0),featured:$('#productFeatured').checked,sold_out:$('#productSoldout').checked,active:$('#productActive').checked,updated_at:new Date().toISOString()};const q=id?supabase.from('store_products').update(payload).eq('id',id):supabase.from('store_products').insert(payload);const {error}=await q;if(error)return msg(error.message);msg('Producto guardado.',true);reset();load()};
$('#resetProduct').onclick=reset;$('#adminProducts').onclick=async e=>{const edit=e.target.closest('[data-edit]');if(edit){const p=products.find(x=>String(x.id)===edit.dataset.edit);if(!p)return;$('#productId').value=p.id;$('#productName').value=p.name;$('#productCategory').value=p.category;$('#productPrice').value=p.price;$('#productOldPrice').value=p.old_price||'';$('#productBadge').value=p.badge||'';$('#productLogo').value=p.logo_url||'';$('#productOrder').value=p.sort_order||0;$('#productFeatured').checked=!!p.featured;$('#productSoldout').checked=!!p.sold_out;$('#productActive').checked=!!p.active;scrollTo({top:0,behavior:'smooth'})}const del=e.target.closest('[data-delete]');if(del&&confirm('¿Eliminar este producto?')){const {error}=await supabase.from('store_products').delete().eq('id',del.dataset.delete);if(error)return msg(error.message);load()}};
$('#offerForm').onsubmit=async e=>{e.preventDefault();await supabase.from('store_offers').update({active:false}).neq('id','00000000-0000-0000-0000-000000000000');const {error}=await supabase.from('store_offers').insert({title:$('#offerTitle').value.trim(),description:$('#offerDescription').value.trim(),ends_at:new Date($('#offerEnds').value).toISOString(),active:$('#offerActive').checked});if(error)return msg(error.message);msg('Oferta guardada.',true)};load();
